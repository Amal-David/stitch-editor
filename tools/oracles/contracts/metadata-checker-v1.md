# Metadata Checker v1

The metadata checker independently reports Rec.709 color intent, sample entry,
media timescales, AAC encoder delay/padding, and the actual codec path. It does
not trust encoder request metadata as evidence of encoded output.
