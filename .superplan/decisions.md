# Decisions

- Record durable workflow or architecture decisions here when they change how future work should proceed.
- 2026-07-14: After attested Terra High cross-review, accept a Rust deterministic editor core with a stable C ABI, Qt Quick desktop shell, and thin Obj-C++/C++ native media/GPU bridges for the vertical slice. Native decode YUV is converted in one GPU pass to bridge-owned RGBA and presented through an opaque frame lease on the Qt render thread. Exclude Go from the real-time engine; MediaBunny is comparison-only. Reopen on failed native, audio, media-I/O, correctness, licensing, or packaging evidence. See `changes/open-video-editor/specs/architecture-decision.md`.
- 2026-07-14: Require GPT-5.6 Terra with high reasoning for all research and implementation sub-agents. Verify each sub-agent's `model`, `reasoning_effort`, `thread_source`, and `agent_path` from `/Users/amal/.codex/state_5.sqlite` before accepting output; parent settings and override responses are not sufficient. The initial council is verified in `changes/open-video-editor/research/terra-high-attestation.md`.
- 2026-07-14: Choose MP4/AVC 8-bit 4:2:0 + AAC-LC as the cross-platform slice baseline with companion WAV/oracle sources. MOV+PCM and 10-bit 4:2:2 remain non-gating probes. Mandatory 3072x1728 AVC/AAC export must pass on both acceptance machines; a typed unsupported result is a slice failure, not a substitute deliverable.
- 2026-07-14: Add a single repository/toolchain bootstrap dependency before parallel foundations and a dedicated cross-platform demux/index/encode/mux/cancel/atomic-export spike before shared preview/export integration. This prevents parallel workers from racing on workspace layout and exposes media-I/O failure before product integration.
