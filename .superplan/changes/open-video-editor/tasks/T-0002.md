---
task_id: T-0002
change_id: open-video-editor
title: Compare Rust, Go, and native cross-platform media-engine architectures
status: done
priority: high
---

## Description
Compare Rust, Go, and any stronger justified alternatives for the core editor and desktop architecture. Evaluate native platform media and GPU APIs, cross-platform abstraction options, FFI cost, threading, memory control, packaging, maintainability, and hardware acceleration on Windows and macOS.

## Acceptance Criteria
- [x] At least two viable end-to-end architecture candidates are described at subsystem level.
- [x] Rust and Go are compared with evidence relevant to real-time media rather than generic language claims.
- [x] Platform media, GPU, audio, UI shell, codec, packaging, and fallback strategies are covered.
- [x] The recommendation identifies components that should be reused, wrapped, or written from scratch.

## Verification
- Read the complete report and checked every acceptance criterion against its decision matrix, subsystem boundaries, and benchmark spikes.
- Checked that the Rust-over-Go conclusion is tied to native API ownership, FFI, real-time audio, and GPU-resource constraints rather than generic benchmarks.

## Evidence
- `research/native-architecture.md`
- `research/terra-high-attestation.md` verifies the author thread as `gpt-5.6-terra`/`high`.
- `research/review-native-architecture.md` and the revised report close the score, time-adapter, Qt render-thread, frame-lease, GPU color-pass, media-I/O, audio, and 3K-export objections.
