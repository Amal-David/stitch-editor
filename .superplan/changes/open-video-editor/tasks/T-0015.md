---
task_id: T-0015
change_id: open-video-editor
title: Bootstrap the open-source repository and pinned cross-platform build contract
status: pending
priority: high
---

## Description
Create the single shared repository/build foundation before parallel implementation begins: Git metadata, GPL-3.0-or-later licensing, directory ownership, pinned Rust/CMake/Qt/native toolchains, dependency policy, CMake/Cargo integration, test entrypoints, CI definitions, and contributor documentation. This task owns shared workspace layout; later workers may not invent competing roots.

## Acceptance Criteria
- [ ] The workspace is a Git repository with documented branch/commit attribution rules, `.gitignore`, `LICENSE`, notices/SBOM policy, contribution and security documents, and no committed generated media or secrets.
- [ ] A documented directory-ownership map separates Rust core/store/contracts, C ABI, Qt desktop shell, macOS bridge, Windows bridge, fixture/oracle tools, benchmarks, packaging, and documentation.
- [ ] Exact supported Rust, CMake, Qt/native-interface, Apple SDK, Windows SDK/MSVC, SQLite, and package-manager inputs are pinned or captured by reproducible lock/config files; Metal and D3D11 backends are asserted rather than inferred.
- [ ] One canonical build/test entrypoint configures Rust plus CMake/Qt without downloading dependencies during the build, and minimal macOS and Windows CI jobs exercise bootstrap, formatting, lint, unit-test discovery, and license/dependency policy.
- [ ] The dependency allowlist excludes FFmpeg, NodeAV, bundled codec binaries, and unreviewed copyleft/license conflicts; the dated legal/distribution gate remains explicit rather than being represented as completed legal advice.
- [ ] Repository bootstrap is reproducible from a clean local checkout, the hosted macOS/Windows workflow is fully defined without build-time dependency downloads, and every implementation sub-agent has authoritative `gpt-5.6-terra`/`high` attestation before material output is accepted. Hosted runner execution evidence is owned by T-0017 and remains a packaging dependency.

## Verification

- Run the canonical configure, build, format, lint, test-discovery, dependency-policy, and clean-checkout smoke commands on macOS; statically validate the hosted workflow. T-0017 executes and accepts both hosted runner legs.
- Validate license/notice files, locked tool versions, forbidden dependency searches, and directory ownership against the architecture contract.
- Record commands, tool versions, CI artifacts, failures, and the Terra High worker attestation.
